map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

map $http_x_request_id $effective_request_id {
    ""        $request_id;
    ~^\s*$    $request_id;
    ~*^\s*(null|undefined|""|'')\s*$ $request_id;
    default   $http_x_request_id;
}
